import { Body } from '../../constants/typography'
import { ButtonContainer } from './styled'

const Button = () => {
  return (
    <ButtonContainer>
      <Body>Logga in</Body>
    </ButtonContainer>
  )
}

export default Button
