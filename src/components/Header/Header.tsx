import { Logo } from '../../constants/typography'
import { HeaderContainer } from './styled'

const Header = () => {
  return (
    <HeaderContainer>
      <Logo>Qlocx</Logo>
    </HeaderContainer>
  )
}

export default Header
